
header#header
  .navbar.navbar-fixed-top(role='navigation')
    .navbar-inner
      .container
        a.brand(href='/') 瑞安鸿瑞包装机械
        .nav-collapse.collapse
          ul.nav
            li
              a(href='/articles', title="article listing") Articles
            li
              a(href="/articles/new", title="new article") New
          ul.nav.pull-right
            if (req.isAuthenticated())
              li
                a(href="/users/"+req.user.id, title="Profile")= req.user.username
              li.dropdown
                a.dropdown-toggle(href='#', role='button', data-toggle='dropdown', data-target='#')
                  i.icon-caret-down
                ul.dropdown-menu(role='menu', aria-labelledby="dLabel")
                  li
                    a(href="/users/"+req.user.id, title="Profile") 购物车
                  li
                    a(href="/users/"+req.user.id, title="Profile") 个人资料
                  li
                    a(href="/logout", title="logout") 注销
            else
              li
                a(href="/login", title="Login") 登陆
